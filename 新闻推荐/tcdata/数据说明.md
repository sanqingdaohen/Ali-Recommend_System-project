# 赛题说明：用户未来点击新闻文章预测

赛题以**预测用户未来点击的新闻文章**为核心任务。数据来自某新闻 APP 平台的真实用户交互日志，包含：

- **30 万用户**
- **近 300 万次点击行为**
- **36 万余篇不同的新闻文章**
- 每篇文章配有对应的 **250 维 embedding 向量**

为保证比赛公平性，数据划分如下：

| 数据集 | 用户数量 | 用途 |
|--------|----------|------|
| 训练集 | 20 万用户 | 模型训练 |
| 测试集 A | 5 万用户 | 公榜评估 |
| 测试集 B | 5 万用户 | 私榜评估 |

---

## 📁 数据文件说明

| 文件名 | 描述 |
|--------|------|
| `train_click_log.csv` | 训练集用户点击日志 |
| `testA_click_log.csv` | 测试集 A 用户点击日志 |
| `articles.csv` | 新闻文章基础信息 |
| `articles_emb.csv` | 新闻文章 embedding 向量（250 维） |
| `sample_submit.csv` | 提交样例文件 |

---

## 📊 字段说明

### 用户点击日志（`train_click_log.csv`, `testA_click_log.csv`）

| 字段名 | 描述 |
|--------|------|
| `user_id` | 用户 ID |
| `click_article_id` | 点击的文章 ID |
| `click_timestamp` | 点击时间戳 |
| `click_environment` | 点击环境 |
| `click_deviceGroup` | 点击设备组 |
| `click_os` | 点击操作系统 |
| `click_country` | 点击国家 |
| `click_region` | 点击地区 |
| `click_referrer_type` | 点击来源类型 |

### 文章信息表（`articles.csv`）

| 字段名 | 描述 |
|--------|------|
| `article_id` | 文章 ID（与 `click_article_id` 对应）|
| `category_id` | 文章类型 ID |
| `created_at_ts` | 文章创建时间戳 |
| `words_count` | 文章字数 |

### 文章 Embedding（`articles_emb.csv`）

| 字段名 | 描述 |
|--------|------|
| `emb_1`, `emb_2`, ..., `emb_250` | 文章的 250 维 embedding 向量表示 |

---

## 📤 结果提交格式

提交文件必须为 **CSV 格式**，且字段顺序与 `sample_submit.csv` 一致：

```csv
user_id,article_1,article_2,article_3,article_4,article_5